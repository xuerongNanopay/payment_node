import { RequestHandler } from "express"

export const addBankAccountByFlinks: RequestHandler = async (req, resp, next) => {
  resp
    .status(600)
    .json({
      message: 'TODO'
    })
}
export const addBankAccountByPlaid: RequestHandler = async (req, resp, next) => {
  resp
    .status(600)
    .json({
      message: 'TODO'
    })
}

export const addBankAccountByVoidCheque: RequestHandler = async (req, resp, next) => {
  resp
    .status(600)
    .json({
      message: 'TODO'
    })
}

export const getBankAccount: RequestHandler = async (req, resp, next) => {
  resp
    .status(600)
    .json({
      message: 'TODO'
    })
}

export const getBankAccounts: RequestHandler = async (req, resp, next) => {
  resp
    .status(600)
    .json({
      message: 'TODO'
    })
}