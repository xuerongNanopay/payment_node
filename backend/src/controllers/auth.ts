import { RequestHandler } from "express"

export const signUp: RequestHandler = async (req, resp, next) => {
  resp
    .status(600)
    .json({
      message: 'TODO'
    })
}

export const logIn: RequestHandler = async (req, resp, next) => {
  resp
    .status(600)
    .json({
      message: 'TODO'
    })
}

export const logOut: RequestHandler = async (req, resp, next) => {
  resp
    .status(600)
    .json({
      message: 'TODO'
    })
}

export const verifyEmail: RequestHandler = async (req, resp, next) => {
  resp
    .status(600)
    .json({
      message: 'TODO'
    })
}

export const changePassword: RequestHandler = async (req, resp, next) => {
  resp
    .status(600)
    .json({
      message: 'TODO'
    })
}

export const getUser: RequestHandler = async (req, resp, next) => {
  resp
    .status(600)
    .json({
      message: 'TODO'
    })
}

export const updateUser: RequestHandler = async (req, resp, next) => {
  resp
    .status(600)
    .json({
      message: 'TODO'
    })
}